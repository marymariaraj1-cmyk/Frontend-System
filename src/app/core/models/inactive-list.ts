export interface InactiveFarmerRow {
  farmerId: string;
  farmerName: string;
  outstandingBalance: number;
}

export interface InactiveBuyerRow {
  buyerId: string;
  buyerName: string;
  outstandingBalance: number;
}